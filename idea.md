# 「家族のおしゃべり」仕様書  
## 非中心性サービス基盤上に構築する、家族専用コミュニケーションシステム

## 1. 概要

「家族のおしゃべり」は、家族間の連絡だけに目的を絞った、極めてシンプルなコミュニケーションアプリである。

一般的なメッセンジャーのように、広告、ニュース、おすすめ、公式アカウント、決済、公開検索、複雑な設定などを持たない。

主な利用想定は、ITリテラシーのある子どもがホストとなり、親や高齢の家族にシンプルなクライアントを提供する形である。

本システムは、単独の中央サービスではなく、利用者自身のCloudflare環境に構築される「非中心性サービス基盤」の最初の実証アプリと位置づける。

---

## 2. 基本理念

### 2.1 根本的なシンプルさ

機能を削って簡単にするのではなく、不要な機能を最初から存在させない。

クライアント側で提供する基本機能は、以下に限定する。

- 家族の会話を見る
- 文字を入力する
- 音声から文字を入力する
- メッセージを送る
- 必要に応じて読み上げる

設定、サーバー、データベース、アカウント、バックアップなどの概念は、原則としてクライアント側に表示しない。

### 2.2 非中心性

非中心性とは、単に中央サーバーを使わないことではない。

本質は、次の関係を実現することである。

> 利用者がプラットフォームに所属するのではなく、プラットフォームから自由である。

提供者が所有する中央データベースに家族の会話を集めるのではなく、各ホストが自分のインフラとデータを持つ。

> ソフトウェアは共有する。  
> インフラは共有しない。  
> データも共有しない。

提供者が提供するものは、主に以下である。

- ソースコード
- 共通API
- PWAフロントエンド
- 管理画面
- デプロイ手順
- 共通モジュール
- 更新と保守

### 2.3 データがアプリに属するのではなく、アプリがデータを利用する

データは特定のアプリに閉じ込めない。

同じデータ基盤を、家族チャット、ブログ、会員制コミュニティ、個人メモ、予約、商品管理など、複数のフロントエンドから利用できるようにする。

---

## 3. 対象ユーザーと役割

### 3.1 ホスト

家族の中でシステム導入と管理を担当するユーザー。

典型的には、ITリテラシーのある子どもがホストになる。

ホストは、複雑な操作を担当してよい。

- Cloudflare環境の作成
- 初期デプロイ
- グループ作成
- メンバー招待
- 権限設定
- 投稿ポリシー設定
- 通知設定
- データ確認
- バックアップ
- エクスポート
- 端末管理
- グループの追加・削除

### 3.2 クライアントユーザー

家族のおしゃべりを利用する一般ユーザー。

特に高齢者の利用を想定し、複雑さをクライアント側から排除する。

理想的な導入手順は以下である。

1. ホストからQRコードを受け取る
2. QRコードを読み取る
3. 通知を許可する
4. ホーム画面に追加する
5. 家族の会話を利用する

電話番号検索、ユーザー名検索、公開プロフィール、「知り合いかも」などは提供しない。

---

## 4. 全体アーキテクチャ

```text
┌─────────────────────┐
│ クライアントPWA       │
│ 家族の会話・投稿       │
└──────────┬──────────┘
           │ HTTPS API
┌──────────▼──────────┐
│ ホスト自身のCloudflare │
│ Worker                │
│ API / 認証 / 通知制御   │
└──────┬──────┬───────┘
       │      │
┌──────▼───┐ ┌▼────────┐
│ D1       │ │ R2      │
│ JSON記録  │ │ 画像等   │
└──────────┘ └─────────┘

       │
┌──────▼──────────┐
│ Web Push基盤     │
│ OSへの呼び鈴通知  │
└─────────────────┘
```

初期版では、Cloudflare WorkersとD1を中心とする。

将来的に画像、音声、動画を扱う場合はR2を追加する。

リアルタイム通信や音声通話が必要になった場合は、Durable Objects、WebSocket、WebRTCなどを追加する。

---

## 5. PWA構成

### 5.1 クライアントPWA

スマートフォンファーストの画面とする。

基本画面は、家族のタイムライン一つである。

画面要素は以下を基本とする。

- 家族名
- 新着メッセージ
- 投稿者名
- 投稿日時
- 入力欄
- 大きなマイクボタン
- 送信ボタン
- 必要に応じた読み上げボタン

グループが一つしかない場合、グループ選択画面は表示しない。

### 5.2 ホスト管理PWA

PCファーストで設計する。

スマートフォンでもレスポンシブに利用できるが、複数グループや設定の管理はPCでの利用を基本とする。

主な機能は以下である。

- グループ一覧
- グループ作成・削除
- グループ名・アイコン設定
- メンバー一覧
- 招待QRコード発行
- 招待コード発行
- メンバー停止・削除
- 端末一覧
- Push登録状態
- 投稿履歴検索
- 投稿削除
- JSONエクスポート
- CSVエクスポート
- バックアップ
- ストレージ使用量
- 投稿ポリシー設定
- テーマ設定

クライアント用PWAと管理用PWAは、同じコードベースを共有してもよい。ただし、表面上は別アプリとして扱うことを推奨する。

例：

- 家族のおしゃべり
- 家族のおしゃべり 管理

クライアント側に管理メニューを存在させないことで、誤操作と情報過多を防止する。

---

## 6. 文脈分離とテーマ/CSS

本システムでは、アカウント切り替えではなく、入口そのものを分ける。

同じ端末上でも、以下を別PWAとして配置できる。

- 自分の家族
- 実家
- 配偶者の実家
- 見守りグループ
- 自分のブログ
- 管理画面

各PWAは、起動時に対象の`space_id`または`group_id`を固定する。

### 6.1 テーマ設定

グループごとにテーマを設定できる。

```json
{
  "theme": {
    "name": "実家",
    "primaryColor": "#2E7D32",
    "accentColor": "#A5D6A7",
    "backgroundColor": "#F1F8E9",
    "icon": "/icons/home-green.png"
  }
}
```

CSS変数の例：

```css
:root {
  --color-primary: #2e7d32;
  --color-accent: #a5d6a7;
  --color-background: #f1f8e9;
}
```

テーマは装飾ではなく、誤投稿防止のための文脈情報として扱う。

> 入口が違う  
> → 見た目が違う  
> → 投稿先も違う

という一貫した関係を成立させる。

色、アイコン、アプリ名、背景、通知名などを固定することで、「今どのグループに投稿しているか」を文字を読まずに認識できるようにする。

---

## 7. データモデル

基盤側では、データの意味を固定しすぎない。

中心となる抽象単位は以下である。

- Space
- Group
- Collection
- Record
- Asset
- Identity
- Permission
- Event
- Subscription

### 7.1 Space

ホストが所有する一つのデータ基盤。

```text
spaces
- id
- owner_id
- name
- created_at
- updated_at
```

### 7.2 Group

家族、実家、町内会、サークルなどの独立した利用単位。

```text
groups
- id
- space_id
- name
- theme_json
- policy_json
- created_at
- updated_at
```

グループ間のデータは完全に分離する。

### 7.3 Collection

Recordのまとまり。

```text
collections
- id
- group_id
- name
- type
- schema_json
- created_at
```

`type`は、アプリ側が意味付けする。

例：

- `conversation`
- `article`
- `product`
- `health-log`
- `event`

### 7.4 Record

実際の構造化データ。

```text
records
- id
- collection_id
- created_by
- data_json
- created_at
- updated_at
- deleted_at
```

チャットメッセージの例：

```json
{
  "type": "message",
  "text": "今日は病院に行ってきました",
  "senderName": "母",
  "attachments": []
}
```

記事の例：

```json
{
  "type": "article",
  "title": "新しい記事",
  "body": "...",
  "tags": ["家族", "日記"]
}
```

基盤側が管理するのは、ID、所有者、コレクション、日時、権限、JSON本体などに限定する。

### 7.5 Asset

画像、音声、動画、添付ファイルなど。

```text
assets
- id
- group_id
- record_id
- object_key
- content_type
- size
- checksum
- created_by
- created_at
```

本文やメタデータはD1、実ファイルはR2に保存する。

---

## 8. 投稿ポリシー

利用可能な機能は、ホストがグループごとに決める。

```json
{
  "allowText": true,
  "allowImage": false,
  "allowAudio": true,
  "allowVideo": false,
  "maxAssetSize": 10485760,
  "retentionDays": null,
  "allowClientDelete": false,
  "requireHostApproval": false
}
```

ポリシーに応じて、クライアント画面に表示するボタン自体を変える。

例：

- 文字のみ
- 文字＋音声入力
- 画像投稿可能
- 動画投稿不可
- 投稿可能なメンバーを限定
- 一定期間後に自動削除

クライアントはポリシーを編集できず、ホストが設定した結果だけを表示する。

---

## 9. Cloudflare構成

### 9.1 Workers

API、認証、権限確認、通知制御を担当する。

主な役割：

- メッセージ取得
- メッセージ投稿
- グループ管理
- メンバー管理
- 招待処理
- Push購読登録
- Push送信
- 管理API
- エクスポート
- ヘルスチェック

### 9.2 D1

初期版の主要データベースとする。

保存対象：

- Space
- Group
- Collection
- Record
- Identity
- Permission
- Device
- Subscription
- Event
- Assetメタデータ

JSON本体を保存しつつ、検索や並び替えに必要な項目だけを通常の列やインデックスとして持つ。

### 9.3 KV

必要に応じて利用する。

用途：

- 短期セッション
- 招待コード
- 一時トークン
- キャッシュ
- レート制限情報

永続的な会話履歴の主保存先にはD1を使用する。

### 9.4 R2

初期版では必須としない。

将来、以下を扱う場合に追加する。

- 写真
- 音声ファイル
- 動画
- PDF
- バックアップファイル

### 9.5 Durable Objects

以下が必要になった段階で導入を検討する。

- グループごとのリアルタイム状態
- WebSocket接続
- オンライン状態
- 通話シグナリング
- 排他制御
- イベント処理

---

## 10. 通知モジュール

通知は「家族のおしゃべり」固有の機能にせず、共通モジュールとして設計する。

通知モジュールは、アプリのデータ内容を理解しない。

扱うのは以下だけである。

- 誰に送るか
- どのグループか
- 何のイベントか
- どのフロントを開くか
- 通知の有効期限
- 購読先の状態

### 10.1 共通API

```http
POST /notify/subscribe
DELETE /notify/subscribe
POST /notify/send
GET /notify/subscriptions
```

送信例：

```json
{
  "target": {
    "groupId": "group_123",
    "userId": "user_456"
  },
  "type": "record.created",
  "title": "新しいおしゃべりがあります",
  "data": {
    "recordId": "record_789",
    "route": "/family/group_123"
  }
}
```

通知本文には、原則として会話本文を含めない。

通知は「呼び鈴」として使い、ユーザーがPWAを開いた後に実データを取得する。

### 10.2 再利用例

同じ通知モジュールを以下に利用できる。

- 家族のおしゃべり：新着メッセージ
- ブログ：新着記事
- 会員制サロン：コメント・返信
- ショップ：注文状況
- 予約システム：予約確認
- 見守り：記録の更新

---

## 11. Web Push

PWAでは、Service Worker、Push API、Notifications APIを組み合わせることで、アプリが前面にない状態でも通知を受け取れる。Push購読情報には通知送信先と暗号化に必要な情報が含まれる。[MDN Push API](https://developer.mozilla.org/en-US/docs/Web/API/Push_API)

iPhone/iPadでも、ホーム画面に追加したWebアプリはWeb Pushに対応している。通知許可はユーザー操作を起点に要求する必要がある。[WebKit Web Push](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/)

ただし、OS内部の通知配送では、iPhoneはAppleのPush基盤、AndroidはブラウザやOSのPush基盤を利用する。

したがって、正確には以下の設計となる。

> アプリストアには依存しない。  
> ただし、OS通知を届けるための基盤までは置き換えない。

通知処理の流れ：

1. クライアントが通知許可を取得
2. PushSubscriptionを作成
3. Workerへ購読情報を登録
4. メッセージ投稿
5. Workerが対象端末へ通知
6. Service Workerが通知を表示
7. ユーザーが通知をタップ
8. PWAが該当グループとメッセージを表示

---

## 12. 認証と権限

初期版では、招待ベースの認証を基本とする。

### 12.1 招待

- QRコード
- 短い招待コード
- 招待の有効期限
- 一度だけ使用可能なトークン
- ホストによる失効

電話番号検索や公開ユーザー検索は行わない。

### 12.2 権限

最低限、以下の権限を定義する。

- `host`
- `member`
- `viewer`
- `moderator`

グループ単位で権限を管理する。

```text
permissions
- id
- group_id
- identity_id
- role
- capabilities_json
- created_at
```

### 12.3 セキュリティ方針

- HTTPS必須
- 招待トークンは短時間で失効
- APIごとにグループ所属を検証
- グループIDを推測可能にしない
- レート制限を導入
- Push本文に機密情報を含めない
- エクスポート操作をホストに限定
- 端末紛失時に端末単位で失効可能にする

---

## 13. 音声機能

### 13.1 音声入力

初期版から優先する機能。

```text
マイクを押す
→ 話す
→ 音声認識
→ 文字として表示
→ 確認
→ 送信
```

送信前に内容を確認できるようにする。

### 13.2 読み上げ

メッセージを音声で読み上げる。

高齢者や視力に不安のあるユーザー向けに、投稿単位で読み上げボタンを提供する。

### 13.3 音声通話

将来拡張とする。

WebRTCを利用し、通常は端末間で音声を直接交換する。

接続不能時にはTURNサーバーを利用する。

WorkerやDurable Objectsは、主に接続情報の交換と着信通知を担当し、音声本体を恒久保存しない。

---

## 14. デプロイ

Cloudflare公式のDeploy to Cloudflareボタンを利用し、ホスト自身のCloudflareアカウントへデプロイできる構成を目指す。

CloudflareのDeployボタンは、Gitリポジトリの取得、Workerの構成、必要なKV・D1・R2などのリソース作成、デプロイまでを数ステップで行える。[Cloudflare Deploy to Cloudflare](https://developers.cloudflare.com/workers/platform/deploy-buttons/)

理想的な導入手順：

1. GitHub上のプロジェクトページを開く
2. 「自分の家族用に設置」を選択
3. Cloudflareへログイン
4. 必要なリソースを自動作成
5. ホスト用URLを発行
6. 管理画面でグループを作成
7. 家族向けQRコードを発行

さらに、AIエージェントを利用できるユーザー向けには、以下のような導入も目指す。

> この家族のおしゃべりを、私のCloudflareアカウントにデプロイしてください。

デプロイ設定はInfrastructure as Codeとして管理し、再構築可能にする。

---

## 15. バックアップと可搬性

非中心性を成立させるため、データの持ち出しを必須機能とする。

### 15.1 エクスポート形式

- JSON
- CSV
- 添付ファイル一式
- メタデータ付きアーカイブ

### 15.2 復元

ホストは、バックアップから別のCloudflare環境へデータを復元できるようにする。

### 15.3 Cloudflare依存の扱い

初期実装はCloudflareを前提とするが、基盤APIとデータ形式はCloudflare固有仕様から分離する。

将来の保存先候補：

- 別クラウド
- NAS
- 自宅サーバー
- ローカル環境
- 別のPaaS
- オブジェクトストレージ

Cloudflareは「現在選択している実装基盤」であり、理念そのものではない。

---

## 16. 初期リリース範囲

### v1

- クライアントPWA
- ホスト管理PWA
- 1グループのタイムライン
- テキスト投稿
- 音声入力
- Web Push通知
- QR招待
- メンバー管理
- D1保存
- JSONエクスポート
- グループテーマ
- Cloudflareデプロイ

### v1.1

- 複数グループ
- グループごとのテーマ
- グループごとの投稿ポリシー
- 端末管理
- 投稿削除
- CSVエクスポート
- 読み上げ

### v2

- 画像添付
- R2連携
- 音声ファイル
- 検索
- 保存期間設定
- バックアップ復元
- WebSocketによるリアルタイム更新

### v3

- WebRTC音声通話
- 会員制グループ
- ブログ・メディア発信
- 商品・注文データ
- 予約管理
- 共通通知SDK
- 複数フロントエンドからの同一データ利用

---

## 17. 将来の非中心性サービス基盤

「家族のおしゃべり」は、単独のメッセンジャーではなく、抽象的な個人データ基盤の利用例である。

同じ基盤上に、以下のサービスを構築できる。

- 家族の日誌
- 見守り記録
- 個人メモ
- 血圧などの健康記録
- ブログ
- 動画メディア
- 会員制サロン
- 小規模コミュニティ
- 町内会
- 商品カタログ
- 注文受付
- 予約管理
- ファイル配布

決済はStripeなどの専門サービスに委任できるが、商品情報、顧客情報、注文履歴は自分のデータ基盤に保存する設計とする。

重要なのは、各サービスを一つの巨大なプラットフォームに統合することではない。

> 一人ひとりが、自分の小さなサービスを持てるようにする。

ことである。

### 17.1 フロントとサーバーを分離するという選択肢

もともと構想の出発点は、クライアント(フロント)とサーバー(API/データ)を分離し、一つの共通フロントが好きなサーバーに接続する形だった。Mastodonのように、フロントは共通の配布物として一箇所で更新し、利用者は自分のサーバーのURLを登録して使う、というイメージである。

「家族のおしゃべり」で実際に作ったものは、これとは違う形になった。1デプロイ(1つのCloudflare Worker)が、フロントとAPIとD1をすべて含み、家族ごとに完全に独立してホストされる。この形は、家族の会話という機微なデータに対しては正しい選択だったと考えている。

- 各家庭のデータが物理的にも別のD1に分かれており、データ主権が明確
- フロントとAPIが同一オリジンなので、httpOnly Cookieによる素朴な認証で済み、特にiOSのPWA(ホーム画面に追加したアプリ)で問題になりやすいクロスオリジンCookie制限を避けられる
- 共通のフロントが存在しないため、フロント配信元が乗っ取られても被害が他の家庭に及ばない(供給網攻撃のリスクがない)

一方で、この形は「毎回デプロイが必要」「更新のたびに各家庭に反映させる手間がかかる」という代償を払っている。データの機微性がそこまで高くない、もっとカジュアルな用途(個人メモ、TODO、ちょっとした記録など)であれば、Mastodon型の「共通フロント + サーバー登録」のほうが柔軟で、利用開始の手間も小さい。

この構成を将来採用する場合、最低限必要になるのは以下である。

- 認証をCookieからBearerトークン(発行したAPIキーをクライアント側でlocalStorage等に保持)に変更する。クロスオリジンでも機能し、iOS Safariの制限も受けない
- サーバー側APIに適切なCORS設定を追加する
- クライアント側に「サーバーのURLを登録する」画面を用意する(Mastodonアプリの「インスタンスを選ぶ」画面に相当)

データモデル(collections / records の汎用JSON構造)自体はこの分離と矛盾しないため、基盤(第2章・第7章)を作り直す必要はなく、フロントとAPIの結合部分だけを分離すればよい。

---

## 18. 成功条件

本プロジェクトの成功は、利用者数や中央データ量の増加だけでは測らない。

以下を成功条件とする。

- 高齢者が迷わず利用できる
- 誤ったグループへの投稿が起こりにくい
- ホストが自分の環境にデプロイできる
- 提供者が会話データを保有しない
- データをJSONとして持ち出せる
- フロントエンドを交換できる
- 通知機能を他のアプリへ再利用できる
- 家族チャット以外のサービスへ拡張できる
- Cloudflare以外の環境へ移行できる

---

## 19. コンセプト

本システムの思想を一文で表すなら、次のとおりである。

> サービスを提供するのではなく、サービスを自分で持てる仕組みを提供する。

「家族のおしゃべり」は、家族の会話という最も身近な用途から、ウェブが本来持っていた非中心性を取り戻すための最初の実証アプリである。
